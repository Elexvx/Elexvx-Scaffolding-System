elexvx:
  web:
    expose-uploads: false
    cors:
      allowed-origin-patterns: ${ELEXVX_CORS_ALLOWED_ORIGINS:${TDESIGN_CORS_ALLOWED_ORIGINS:}}

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
