<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.elexvx.admin.mapper.SensitiveSettingMapper">
  <select id="selectTop" resultType="top.elexvx.admin.entity.SensitiveSetting">
    SELECT id, enabled, updated_at
    FROM sensitive_settings
    ORDER BY id ASC
    <choose>
      <when test="_databaseId == 'sqlserver'">
        OFFSET 0 ROWS FETCH NEXT 1 ROWS ONLY
      </when>
      <when test="_databaseId == 'oracle'">
        FETCH FIRST 1 ROWS ONLY
      </when>
      <otherwise>
        LIMIT 1
      </otherwise>
    </choose>
  </select>

  <insert id="insert" parameterType="top.elexvx.admin.entity.SensitiveSetting" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO sensitive_settings (enabled, updated_at)
    VALUES (#{enabled}, #{updatedAt})
  </insert>

  <update id="update" parameterType="top.elexvx.admin.entity.SensitiveSetting">
    UPDATE sensitive_settings
    SET enabled = #{enabled},
      updated_at = #{updatedAt}
    WHERE id = #{id}
  </update>
</mapper>
